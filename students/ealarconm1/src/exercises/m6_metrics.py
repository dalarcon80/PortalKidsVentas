
import pandas as pd

def metrics(customers: 'pd.DataFrame', products: 'pd.DataFrame', orders: 'pd.DataFrame'):
    # TODO: revenue = quantity*price; devuelve (ventas_por_dia, top_productos, ventas_por_ciudad)
    raise NotImplementedError("Implementa metrics")
