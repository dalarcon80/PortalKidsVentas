
import os
import pandas as pd

def to_silver(role: str, customers: 'pd.DataFrame', products: 'pd.DataFrame', orders: 'pd.DataFrame', base_dir: str):
    # TODO: normalizar tipos y escribir *_v_clean.csv o *_o_clean.csv
    raise NotImplementedError("Implementa to_silver")
